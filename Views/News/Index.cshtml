@model List<CEPAW.NewsApp.Models.NewsArticle>

<h1>Latest News</h1>

@foreach (var article in Model)
{
    <div>
        <h2>@article.Title</h2>
        <p>@article.Description</p>
        <a href="@article.Url">Read more</a>
        <p>Published: @article.PublishedDate</p>
    </div>
}
